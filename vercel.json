{
  "buildCommand": "pnpm --filter site-en build",
  "outputDirectory": "packages/site-en/dist",
  "installCommand": "pnpm install", // Use pnpm for installation
  "devCommand": "pnpm run dev:en",  // Optional: use pnpm for dev command
  "framework": "astro"
}
